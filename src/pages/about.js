import React from 'react';

const AboutPage = () => {
    return(
        <React.Fragment>
            <h1>About Page</h1>
        </React.Fragment>
    );
};

export default AboutPage;

//importing react, creating a variable on the page, making that variable an arrow function and exporting the variable are all the standard 
// for creating a new page